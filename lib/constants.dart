import 'package:flutter/material.dart';

// TODO: Infer colors **ONLY** from Theme
const main_col = Color(0xFF01ACF1);
final second_col = Color(0xFFE1E6FF);
// TODO: Use spacings only in UI code
final double std_padding = 17;
